// import {createSelector} from "reselect";

export const getUserProfileSel = (state) => state.profile.userProfile;
export const getUserId = (state) => state.auth.userId;
export const getStatusSelector = (state) => state.profile.status;
